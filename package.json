{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "A portfolio website with a React frontend and Node.js backend",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd reactapp && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@google-cloud/recaptcha-enterprise": "^5.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "nodemailer": "^6.9.11"
  }
}